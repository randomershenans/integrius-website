/**
 * Integrius Client Portal — API Tests
 *
 * Architecture: Supabase Auth for clients, custom JWT (requireAdmin) for admin routes.
 *
 * Run: npx jest src/__tests__/portal/api.test.ts
 * Requires: npm i -D jest ts-jest @types/jest
 *
 * NOTE: @types/jest is not yet installed. This file is a placeholder.
 * The tsconfig excludes __tests__ from default compilation until jest is set up.
 */

// Stub export so TypeScript treats this as a module, not a script.
export {}
